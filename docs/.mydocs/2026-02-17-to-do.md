# OpenClaw To-Do — 2026-02-17

## Open Items

### 1. Fix `/new` command authorization in Discord channels

**Priority:** High
**Context:** Sending `/new` in any Discord channel returns an authorization error. This prevents session resets from Discord — currently the only workaround is CLI access to the VM (`openclaw sessions reset` or deleting the session store directly).

**Investigation leads:**

- Check if Discord user ID needs to be added to an `allowFrom` list in the guild config
- Check `commands` config section for permission settings
- Run `openclaw docs commands` on the VM for command permission documentation
- May need an `elevated` or `operator` config per agent

### 2. Fix `channels unresolved` warning on startup

**Priority:** Low (not blocking functionality)
**Context:** All 4 channels show as "unresolved" at startup: `[discord] channels unresolved: 1471887317315752151/1471887664491008143, ...`. The bot still works, but this may indicate a timing issue or missing permissions.

**Investigation leads:**

- May just be a race condition — Discord gateway hasn't sent `GUILD_CREATE` yet when the log fires
- Run `openclaw channels health --verbose` to check live resolution
- Verify bot has VIEW_CHANNEL permission for all 4 channels in Discord server settings

### 3. Fix Tailscale proxy trust for web UI access

**Priority:** Medium
**Context:** Browser connections via Tailscale serve are rejected with `pairing required` or `connect failed` because the gateway doesn't trust proxy headers from `127.0.0.1` (Tailscale serve forwards).

**Fix:** Add to `gateway` config:

```json
"trustedProxies": ["127.0.0.1"]
```

### 4. Clean up stale session store

**Priority:** Low
**Context:** Old sessions under `agent:main:*` may still exist in `/home/prill/.openclaw/agents/main/sessions/sessions.json`. These are from before multi-agent routing was working and can be safely removed.

### 5. Add AGENTS.md files to agent workspaces

**Priority:** Low
**Context:** Each agent workspace can have an `AGENTS.md` file defining tool usage policies, coding standards, and behavioral rules specific to that agent's domain. Currently only SOUL.md files are placed.

### 6. Consider adding `openclaw-updated.json` to .gitignore

**Priority:** Low
**Context:** The working config file contains real API tokens (Discord bot token, gateway auth token). It's useful as a reference but should not be committed with real credentials. Either gitignore it or ensure tokens are redacted before committing.
